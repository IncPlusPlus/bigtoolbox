# Add a label for release-drafter to ignore this commit as it is probably just being used to get master/develop up to speed with the other
skip-changelog:
  head: "master||develop"
  base: "develop||master"
